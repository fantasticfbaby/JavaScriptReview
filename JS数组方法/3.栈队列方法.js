var colors = new Array()
var count = colors.push('red', 'green') // return the length of array pushed
console.log(count)
console.log(colors.pop()) // return the element poped

console.log(colors.shift()) // equals to pop front
count = colors.unshift('blue') // equals to push front
console.log(count)